apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dbaas-api-tls
  namespace: dbaas-api

spec:
  secretName: dbaas-api-tls

  issuerRef:
    name: letsencrypt-prod
    kind: Issuer

  commonName: "api.foyez.runs.onstackit.cloud"
  duration: 8760h0m0s

  dnsNames:
    - "api.foyez.runs.onstackit.cloud"